The goal of the second lab was to learn a bit more advanced BASH commands, such as grep or wc, and to dive a bit into creating BASH scripts. To do so, we created a small script for parsing FASTA files.

This directory contains a 'notebook' subdirectory.

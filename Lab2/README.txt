The goal of the second lab was to learn a bit more advanced BASH commands, such as grep or wc, and to dive a bit into creating BASH scripts. The means for it were creating a script for parsing sample FASTA files.

This directory contains a 'notebook' subdirectory.
